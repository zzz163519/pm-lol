[project]
name = "pm-lol"
version = "0.1.0"
description = "Polymarket LoL data foundation spike"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
